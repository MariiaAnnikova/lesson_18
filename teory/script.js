//let a = 10;
////
//setTimeout(() => {
    //a= 34;
  //  console.log(a);
///}, 2000)
//console.log(a);
//let example = new Promise((resolve, reject) => {
 //   setTimeout(() => {
 //       resolve(a=34);
 //   }, 2000)
//});
//example.then(() => console.log(a));
//const random_num = new Promise((resolve, reject) =>{
 //    if(Math.random() >= 0.5) {
 //       resolve()
  //   } else {
  //      reject()
  //   }
//})
//random_num.then(
   // ()=> console.log('< 0.5')
   //()=> console.log('>= 0.5')
//)

//const random_numsi = new Promise((resolve, reject) =>{
   // if(Math.random() >= 0.5) {
    //   resolve(a)
    //} else {
    //   reject()
   // }
//})
//random_numsi.then(
   //(value)=> console.log('>= 0.5' +  value),
  // (value)=> console.log(`the number ${value} is more than 0.5`),
  // () => console.log('< 0.5')
//)/

//const random_numsis = new Promise((resolve, reject) =>{
   // if(Math.random() >= 0.5) {
 //      resolve(a)
   // } else {
   //    reject(a)
  //  }
//})
//andom_numsis.then(
   //(value)=> console.log('>= 0.5' +  value),
  // (value)=> console.log(`the number ${value} >= 0.5`),
  //(value)=> console.log(`the number ${value} < 0.5`),
//)

